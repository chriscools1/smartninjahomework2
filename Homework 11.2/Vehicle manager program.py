class Vehicle:
    count_vehicle = 0

        def __init__(self, merk, model, km_gereden, datum_onderhoud):
            self.merk = merk
            self.model = model
            self.km_gereden = km_gereden
            self.datum_onderhoud = datum_onderhoud  # moet een string zijn in het formaat "DD.MM.YYYY"


        def aantal_wagens(self)
            return self.count_vehicle

        def edit_km(self, new_km):
            self.km_gereden += new_km

        def verander_datum_onderhoud(self, nieuwe_datum):
            self.datum_onderhoud = nieuwe_datum

class VehicleManager:






